[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
  NPM_VERSION = "10"

# Direct all route requests to index.html so Vite's client-side router handles navigation
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  force = false
